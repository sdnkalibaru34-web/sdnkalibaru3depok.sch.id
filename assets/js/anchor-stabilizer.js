(()=>{const getTarget=()=>{if(!location.hash)return null;try{return document.getElementById(decodeURIComponent(location.hash.slice(1)))}catch{return null}};const getOffset=()=>{const header=document.querySelector('.site-header');const h=header?header.getBoundingClientRect().height:0;const gap=window.innerWidth<=640?28:window.innerWidth<=900?34:38;return h+gap};const place=()=>{const target=getTarget();if(!target)return;const top=target.getBoundingClientRect().top+window.scrollY-getOffset();window.scrollTo({top:Math.max(0,top),behavior:'auto'})};const stabilize=()=>{const target=getTarget();if(!target)return;let last=-1,stable=0,frames=0;const tick=()=>{const top=Math.round(target.getBoundingClientRect().top+window.scrollY);if(Math.abs(top-last)<=1)stable++;else stable=0;last=top;frames++;if(stable>=6||frames>=180){place();setTimeout(place,120);setTimeout(place,350);return}requestAnimationFrame(tick)};requestAnimationFrame(tick)};document.addEventListener('DOMContentLoaded',()=>{if(!location.hash)return;place();stabilize();document.querySelectorAll('img').forEach(img=>{if(!img.complete)img.addEventListener('load',place,{once:true})});const dynamic=document.querySelector('main');if(dynamic&&'ResizeObserver'in window){let timer;const ro=new ResizeObserver(()=>{clearTimeout(timer);timer=setTimeout(place,80)});ro.observe(dynamic);setTimeout(()=>ro.disconnect(),4000)}});window.addEventListener('load',()=>{place();setTimeout(place,120)});window.addEventListener('hashchange',()=>{place();stabilize()})})();